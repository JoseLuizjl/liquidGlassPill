#pragma once

struct LiquidGlassPushConstants {
    float resolution[2];
    float time;
    float dragging;
    float pillCenter[2];
    float pillSize[2];
    float gooAmount;
    float edgeDockSide;
    float desktopMode;
    float buttonCenterX;
    float buttonCenterY;
    float buttonWidth;
    float buttonHeight;
};
